## 1. Matrix Sources

- [x] 1.1 Add a repo-local verification inventory/report tool that scans registered tests, baseline gate entrypoints, probe scripts, and evidence directories.
- [x] 1.2 Add a checked-in verification matrix that summarizes L1/L2/L3/L4 coverage and constrained gaps for every current formal capability.

## 2. Spec And Documentation Alignment

- [x] 2.1 Add the verification-evidence delta spec for the formal verification matrix and inventory/report tool.
- [x] 2.2 Update repository documentation indexes so reviewers can find the matrix and inventory entrypoint.

## 3. Evidence And Validation

- [x] 3.1 Add a verification record capturing the inventory output and the matrix review scope.
- [x] 3.2 Run the inventory tool, `openspec validate verification-matrix-v1`, and `openspec validate --specs`.

## 4. Finalization

- [x] 4.1 Archive the change after the matrix and inventory tooling are aligned.
- [x] 4.2 Commit the verification-matrix update using the repository's Conventional Commit rule.
